---
description: 
globs: 
alwaysApply: true
---
1. Always use Venv to run command and install library
2. Ensure all code and dependencies are compatible with ARMv8 architecture (Orange Pi 5 Pro) running Debian (aarch64). 

3. All newly used Python packages must be added to requirements.txt immediately, either manually or via pip freeze > requirements.txt update.